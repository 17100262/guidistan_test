<%= form_for(@subdiscipline) do |f| %>
  <% if @subdiscipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subdiscipline.errors.count, "error") %> prohibited this subdiscipline from being saved:</h2>

      <ul>
      <% @subdiscipline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :discipline_id %>
    <%= f.collection_select :discipline_id, Discipline.all,:id,:name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
