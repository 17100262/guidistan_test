<%= bootstrap_form_for(@subdiscipline) do |f| %>
  <% if @subdiscipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subdiscipline.errors.count, "error") %> prohibited this subdiscipline from being saved:</h2>

      <ul>
      <% @subdiscipline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, placeholder: "Name", hide_label: true  %>
  </div>
  <div class="field">
    <%= f.collection_select :discipline_id, Discipline.all,:id,:name %>
  </div>
  <div class="field">
    <%= f.text_area :description, placeholder: "Description goes here", hide_label: true  %>
  </div>
  <div class="actions">
    <%= link_to 'Cancel', subdisciplines_path, class: "btn btn-info"  %>
    <%= f.submit "Create", class: "btn btn-success" %>
  </div>
<% end %>
