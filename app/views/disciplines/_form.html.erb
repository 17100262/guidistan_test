

<%= bootstrap_form_for(@discipline) do |f| %>
  <% if @discipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discipline.errors.count, "error") %> prohibited this discipline from being saved:</h2>

      <ul>
      <% @discipline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <!--<= f.label :name %><br>-->
    <%= f.text_field :name, placeholder: "Name", hide_label: true %>
  </div>
  <div class="field">
    <!--<= f.label :description %><br>-->
    <%= f.text_area :description, placeholder: "Description goes here", hide_label: true %>
  </div>
  <div class="field">
    <!--<= f.label :name %><br>-->
    <%= f.text_field :font, placeholder: "Font", hide_label: true %>
  </div>
  <div class="field">
  <!--<= f.label :Image %>-->
  <%= f.file_field :image %>
  </div>
  <!--<div class="actions">-->
  <%= link_to 'Cancel', disciplines_path, class: "btn btn-info" %>
    <%= f.submit "Create", class: "btn btn-success" %>
  <!--</div>-->
<% end %>
